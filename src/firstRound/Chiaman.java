package firstRound;

public class Chiaman {

	public int nearestWarehouseEnough(int x, int y, int pType, int nItems) {
		for (int i = 0; i < Q.W; i++) {
//			if (Q.warehouses[i].products[pType] >= nItems && ) {
				
			}
		}
	}
}
